{
	"name": "csv",
	"version": "1.0.0",
	"description": "Script to filter data from CSV based on a given input",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "Saj",
	"license": "ISC",
	"dependencies": {
		"papaparse": "^5.3.0"
	}
}
